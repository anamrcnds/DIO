"use strict";
// interface IUsuario {
//     id: string,
//     email: string,
// }
function redirecione(usuario) {
    if (usuario.cargo) {
        // redirecionar(usuario.cargo);
    }
    //redirecionar para a área do usuário
}
